@page
@model CandyHeaven.Pages.ShoppingCart.ShippingModel
@{
}

<form method="post" asp-page="Payment">
    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th colspan="2">Produkt</th>
                <th class="text-center">Antal</th>
                <th>Summa</th>
            </tr>
        </thead>
        @foreach (var product in Model.ShoppingCart)
        {
            <tr>
                <td><img src="@product.ImageURL" alt="@product.Name image" /></td>
                <td>@product.Name <br />@(product.StockQuantity > 0 ? "Finns i lager" : "Ej i lager")</td>
                <td>
                    <a asp-page="ShoppingCart" asp-route-removeId="@product.Id" class="btn btn-outline-dark">-</a>
                    <a asp-page="ShoppingCart" asp-route-addId="@product.Id" class="btn btn-outline-dark">+</a>

                </td>
                @*<td><input type="number" name="quantities" value="@product.StockQuantity" min="1" style="width: 50px;" /></td>*@
                <td>@product.Price kr</td>
            </tr>
        }
    </table>
</form>

<form  method="post">

    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th colspan="2">Leveranssätt</th>
                <th>Pris</th>
            </tr>
        </thead>
        <tr>
            <td>
                @*Förnamn: <input asp-for="@Model.Name" />
                Efternamn: <input asp-for="@Model.LastName" />
                Email:<input asp-for="@Model.Email" />
                Adress:<input asp-for="@Model.Adress" />*@
                Förnamn: <input asp-page="Shipping" asp-route-name="@Model.Name"/>
                Efternamn: <input asp-for="@Model.LastName" />
                Email:<input asp-for="@Model.Email" />
                Adress:<input asp-for="@Model.Adress" />

                <p>Välj leveranssätt:</p>

                <input type="radio" id="html" name="frakt" value="">
                <label for="html">DHL Express</label><br>

                <input type="radio" id="css" name="frakt" value="CSS">
                <label for="css">Instabox</label><br>


                <input type="radio" id="javascript" name="frakt" value="JavaScript">
                <label for="javascript">Hämta i butik</label>
                <a asp-for="Payment" asp-route-id="39"></a>

                <a type="submit" class="btn btn-outline-dark btn-lg" href="Payment" role="button">FORTSÄTT</a>
            </td>
        </tr>
    </table>
</form>
